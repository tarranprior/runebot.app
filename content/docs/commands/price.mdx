---
title: Price
---

The `/price` command returns item price information from the Grand Exchange.

## Usage

```text
/price [search_query]
```

### Example

```
/price search_query: Old school bond
```

<DocsPricePreview className="mt-12 mb-12" />

Use the buttons below the embed to **visit external data sources** (opens in browser), and use the <kbd>⟳</kbd> button to **refresh price data**.

<br/>
